---
description: v1.6.6 | Query schedule via Oracle API (Drizzle DB). Use when user says "schedule", "upcoming events", "what's on today", "calendar".
---

# /schedule

Execute the `schedule` skill with the provided arguments.

## Instructions

**If you have a Skill tool available**: Use it directly with `skill: "schedule"` instead of reading the file manually.

**Otherwise**:
1. Read the skill file at this exact path: `~/.claude/skills/schedule/SKILL.md`
2. Follow all instructions in the skill file
3. Pass these arguments to the skill: `$ARGUMENTS`

**IMPORTANT**: Only use the path above. Do NOT search for or use any other file with "schedule" in the name.

---
*oracle-skills-cli v1.6.6*
